{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Remote-Debug",
            "type": "shell",
            "command": "./remote-debug.sh"
        },
        {
            "label": "Remote-Debug-Windows",
            "type": "shell",
            "command": "./build.ps1; ./deploy.ps1"
        }
    ]
}